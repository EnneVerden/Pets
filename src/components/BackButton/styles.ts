import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    top: 10,
    left: 10,
    zIndex: 100,
    width: 30,
    height: 40,
    borderRadius: 30,
    backgroundColor: '#ffffff',
  },
})
